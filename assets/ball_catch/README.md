# Ball catch assets (future)

Reserved for shared props used by both Isaac training and Unity XR:

- `ball.usd` — visual mesh + collider (optional replacement for procedural sphere)
- `catch_arena.usd` — floor, safety cage, throw zone markers
- textures / materials

Isaac task currently spawns a physics sphere in code; point `ball_catch_env_cfg.py` at a USD here when ready.
